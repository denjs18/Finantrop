# Phase 8 — à implémenter après validation phases 1-7
from typing import Optional


def generate_ai_analysis(
    ticker: str,
    stock_info: dict,
    indicators: dict,
    prediction: dict,
    sentiment: dict,
) -> Optional[dict]:
    """
    Placeholder — implemented in Phase 8.
    Will call Claude API to synthesise all data into a structured verdict.
    """
    raise NotImplementedError("Phase 8 not yet implemented")
